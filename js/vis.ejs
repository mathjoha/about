```{=html}
<ul>
  <% for (const item of items) { %>
    <li>
      <strong><a href="<%= item.url %>"><%= item.title %></a></strong> — <%= item.context %><br>
      <small><%= item.description %></small>
    </li>
  <% } %>
</ul>
```
